interface:
  display_name: "Shiplight Cloud Test Management"
  short_description: "Cloud test case management — create, run, and manage tests via Shiplight API"

policy:
  allow_implicit_invocation: true
